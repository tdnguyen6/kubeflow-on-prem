---
bases:
- ../../helmfile.yaml
---
repositories:
- name: dex
  url: https://charts.dexidp.io

releases:
- name: dex
  namespace: auth
  version: 2.30.0
  values:
  ingress:
    tls:
    - secretName: self-signed-tls-secret
      hosts:
        - a.io